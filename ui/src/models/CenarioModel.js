export default class CenarioModel {
  constructor(id, x, y, student, prediction) {
    this.id = id;
    this.x = x;
    this.y = y;
    this.student = student;
    this.prediction = prediction;
  }
}